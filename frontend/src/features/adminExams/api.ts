import { examsAdminApi } from "@/lib/api";

/**
 * Feature API for staff exam authoring + assignment console.
 * Admin pages should import from here, not from `lib/api`.
 */
export const adminExamsFeatureApi = examsAdminApi;

